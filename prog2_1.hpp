#ifndef __PROG2_1_HPP__
#define

    class Tokenizer{
    
        public:
            Tokenizer();
            ~Tokenizer();
            void Tokenize(string s);
            vector<string> GetTokens();
    };
#endif
